apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: dev

namePrefix: dev-
nameSuffix: ""

patches:
  - path: replicas-patch.yaml
    target:
      kind: Deployment
      name: dotnet-api