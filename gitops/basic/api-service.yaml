apiVersion: v1
kind: Service
metadata:
  name: dotnet-api
  namespace: argo-demo
spec:
  type: ClusterIP
  selector:
    app: dotnet-api
  ports:
  - port: 3000
    targetPort: 8000
    protocol: TCP
